{
  "name": "kairo-widget",
  "version": "0.1.0",
  "private": true,
  "description": "KAIRO embeddable WebChat widget. Bundles Shadow DOM widget served from widget.kairoagent.com.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node ./scripts/copy-index.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
